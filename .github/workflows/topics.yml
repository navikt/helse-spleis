name: Aiven config for de andre topicene våre

on:
  push:
    branches:
    - master
    paths:
    - 'deploy/topic-avviksvurdering-migreringer-dev.yml'
    - 'deploy/topic-avviksvurdering-migreringer-prod.yml'

jobs:
  deployDev:
    name: apply dev-gcp
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: nais/deploy/actions/deploy@v1
        env:
          APIKEY: ${{ secrets.NAIS_DEPLOY_APIKEY }}
          CLUSTER: dev-gcp
          RESOURCE: deploy/topic-avviksvurdering-migreringer-dev.yml

  deployProd:
    name: apply prod-gcp
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: nais/deploy/actions/deploy@v1
        env:
          APIKEY: ${{ secrets.NAIS_DEPLOY_APIKEY }}
          CLUSTER: prod-gcp
          RESOURCE: deploy/topic-avviksvurdering-migreringer-prod.yml
